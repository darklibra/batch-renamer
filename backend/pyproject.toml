[project]
name = "clear-file-backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "sqlalchemy"
]

[project.optional-dependencies]
test = [
    "pytest",
    "httpx"
]
