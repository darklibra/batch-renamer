# 구현 계획서

## 1. 기능 요구사항

- **파일 스캔**: 지정된 폴더에서 파일 목록을 읽어옵니다.
- **정보 추출**: 파일 이름에서 날짜와 같은 특정 정보를 파싱합니다.
- **파일 복사**: 추출된 정보를 기반으로 파일을 새 위치에 복사합니다.
- **백엔드**: Python, FastAPI, SQLite를 사용하여 API를 구축합니다.
- **프론트엔드**: React를 사용하여 사용자 인터페이스를 구축합니다.
- **모노레포**: 백엔드와 프론트엔드 코드를 하나의 저장소에서 관리합니다.

## 2. API 명세

### GET /api/files
- **설명**: 지정된 소스 폴더의 파일 목록을 가져옵니다.
- **쿼리 파라미터**: `path: str` (소스 폴더 경로)
- **응답**:
  ```json
  [
    {
      "name": "IMG_20230810_123456.jpg",
      "path": "/source/IMG_20230810_123456.jpg",
      "parsed_info": {
        "date": "2023-08-10",
        "time": "12:34:56"
      }
    }
  ]
  ```

### POST /api/files/copy
- **설명**: 파일을 새 경로로 복사합니다.
- **요청 본문**:
  ```json
  {
    "source_path": "/source/IMG_20230810_123456.jpg",
    "destination_path": "/organized/2023/08/IMG_20230810_123456.jpg"
  }
  ```
- **응답**:
  ```json
  {
    "status": "success",
    "message": "File copied successfully."
  }
  ```

## 3. 개발 태스크 목록

| 상태 | 태스크 | 담당자 |
| --- | --- | --- |
| **To Do** | **[공통]** Docker 설정 (docker-compose.yml) | |
| **To Do** | **[Backend]** `pyproject.toml` 설정 및 `uv` 환경 구성 | |
| **To Do** | **[Backend]** `.env` 파일을 이용한 환경 변수 설정 | |
| **To Do** | **[Backend]** SQLite 데이터베이스 초기화 스크립트 (`scripts/init_db.py`) 작성 | |
| **To Do** | **[Backend]** 파일 스캔 서비스 로직 구현 (TDD) | |
| **To Do** | **[Backend]** 파일명 파싱 유틸리티 구현 (TDD) | |
| **To Do** | **[Backend]** 파일 복사 서비스 로직 구현 (TDD) | |
| **To Do** | **[Backend]** `/api/files` GET 엔드포인트 구현 | |
| **To Do** | **[Backend]** `/api/files/copy` POST 엔드포인트 구현 | |
| **To Do** | **[Frontend]** React 프로젝트 초기 설정 (`create-react-app` 또는 `vite`) | |
| **To Do** | **[Frontend]** `package.json` 의존성 설정 | |
| **To Do** | **[Frontend]** `.env` 파일을 이용한 환경 변수 설정 | |
| **To Do** | **[Frontend]** 파일 목록을 표시할 `FileList` 컴포넌트 생성 | |
| **To Do** | **[Frontend]** 백엔드 API와 통신할 `useFiles` 커스텀 훅 생성 | |
| **To Do** | **[Frontend]** 파일 복사 요청을 보낼 UI 구현 | |
| **In Progress** | | |
| **Done** | **[공통]** 모노레포 구조 설정 | Gemini |
| **Done** | **[Backend]** FastAPI 프로젝트 초기 설정 | Gemini |
| **Done** | | |

## 4. 아키텍처 분석 (/sg:analyze --architect)

### Phase 1: 입력 및 스캔
- **입력**: 사용자가 로컬 폴더 경로를 제공합니다.
- **스캔**: 시스템은 제공된 폴더와 모든 하위 폴더를 재귀적으로 탐색하여 파일 목록을 가져옵니다.

### Phase 2: 패턴 분석 및 추출 전략 수립
- **개요**: 각 파일의 이름에 여러 패턴을 적용해보고, 그 중 가장 많은 정보를 성공적으로 추출하는 최적의 패턴을 선택하여 적용합니다.
- **패턴 매칭**:
  - 시스템은 미리 정의된 또는 사용자가 추가한 여러 파일명 패턴 목록을 가집니다.
  - 각 파일마다 목록에 있는 모든 패턴을 순차적으로 대입해 봅니다.
- **최적 패턴 선택**:
  - 각 패턴이 파일명에서 추출할 수 있는 데이터의 개수(예: '년', '월', '일', '주제' -> 4개)를 계산합니다.
  - 가장 많은 수의 데이터를 성공적으로 추출한 패턴을 해당 파일의 최종 패턴으로 선택합니다.
- **추출 규칙 정의**: 이 과정을 모든 파일에 대해 수행하여, 각 파일에 가장 적합한 개별 추출 규칙을 동적으로 결정합니다.

### Phase 3: 복사 계획 수립 및 사용자 확인
- **계획 생성**: 정의된 추출 규칙을 사용하여 각 파일을 어떤 목적지 경로로 복사할지에 대한 계획을 생성합니다.
  - 예: `IMG_20230101_120000.jpg` -> `/정리된 폴더/2023/01/IMG_20230101_120000.jpg`
- **사용자 확인**: 생성된 복사 계획을 사용자에게 먼저 표시하여 실행 전 확인을 받습니다. 이 단계에서 사용자는 개별 항목을 수정하거나 제외할 수 있습니다.

### Phase 4: 실행 및 로깅
- **실행**: 사용자 확인 후, 시스템은 복사 계획에 따라 파일 복사를 실행합니다.
- **로깅**: 각 파일 복사 작업의 결과(성공, 실패, 실패 시 원인)를 데이터베이스나 로그 파일에 기록합니다.

### Phase 5: 결과 저장 및 보고
- **결과 저장**: 모든 실행 결과를 SQLite 데이터베이스에 저장하여 추적 및 관리가 가능하도록 합니다.
- **보고**: 최종 결과를 프론트엔드 UI를 통해 사용자에게 명확하게 표시합니다. 성공적으로 복사된 파일과 실패한 파일 목록을 보여줍니다.
